[setup]
install = """
python3 -m venv /opt/venv
. /opt/venv/bin/activate
pip install -r requirements.txt
"""

[start]
command = "cd /opt/app && mkdir -p logs && /opt/venv/bin/python chainlink_price_service.py"
