# Enhanced Price Collector Service

**Улучшенный сервис для сбора цен с поддержкой ET времени и буферизации**

## 🚀 Новые функции

### ✨ ET (Eastern Time) Поддержка
- Автоматическая конвертация времени между UTC и ET
- Отображение времени в обоих форматах в API ответах
- Настройка retention по ET времени

### 📊 Буферизация для актуальных данных
- **In-memory buffer** для цен, сгенерированных в последние 60 секунд
- **Приоритет буфера**: Сначала ищет в буфере, потом в файле
- **Решает проблему**: Теперь можно получить цену, которая еще не записана в файл

### 🧹 Улучшенная очистка данных
- Настройка retention от 6 до 12 часов
- Автоматическая очистка каждые 10 минут
- Поддержка ET времени при очистке

## 📁 Структура проекта

```
Price collector_service/
├── enhanced_price_service.py    # Улучшенный сервис с ET и буферизацией
├── enhanced_config.json         # Конфигурация с новыми настройками
├── test_enhanced_collector.py   # Тесты для новых функций
├── requirements.txt             # Зависимости (включая pytz)
├── simple_price_service.py      # Оригинальный сервис
├── config.json                  # Оригинальная конфигурация
├── README.md                    # Оригинальная документация
├── data/                        # Данные
│   └── prices.json
└── logs/                        # Логи
```

## ⚙️ Конфигурация

### Новые параметры в `enhanced_config.json`:

```json
{
  "data_retention_hours": 12,           // Сколько часов хранить данные
  "buffer_max_age_seconds": 60,         // Сколько секунд хранить в буфере
  "features": {
    "et_time_support": true,            // Включить ET время
    "buffering": true,                  // Включить буферизацию
    "enhanced_cleanup": true            // Включить улучшенную очистку
  }
}
```

## 🚀 Запуск

### Локально
```bash
# Установка зависимостей
pip install -r requirements.txt

# Запуск улучшенного сервиса
python enhanced_price_service.py
```

### На VPS
```bash
# Остановка старого контейнера
ssh root@danunahbot.ru "docker stop pricecollector-service"

# Запуск нового контейнера с улучшенным сервисом
ssh root@danunahbot.ru "docker run -d --name pricecollector-service -p 8080:8080 -v /app/data:/app/data -v /app/logs:/app/logs pricecollector-service-eqkne3:latest"
```

## 📡 API Эндпоинты

### 1. Health Check
```bash
GET /health
```
**Ответ:**
```json
{
  "status": "ok",
  "timestamp": 1771078413,
  "source": "enhanced",
  "symbols": ["BTC", "ETH", "SOL"],
  "features": ["et_time_support", "buffering", "enhanced_cleanup"],
  "time_info": {
    "utc": "2026-02-14 14:13:33 UTC",
    "et": "2026-02-14 09:13:33 EST"
  }
}
```

### 2. Получение цены по времени (с буферизацией!)
```bash
GET /price/BTC?timestamp=1771078231&tolerance=60
```
**Ответ:**
```json
{
  "symbol": "BTC",
  "price": 49118.59,
  "timestamp": 1771078231,
  "requested_timestamp": 1771078231,
  "round_id": 1024,
  "source": "buffer",  // или "file"
  "time_info": {
    "utc": "2026-02-14 14:10:31 UTC",
    "et": "2026-02-14 09:10:31 EST"
  }
}
```

### 3. Последние цены
```bash
GET /latest
```
**Ответ:**
```json
{
  "prices": [
    {
      "symbol": "BTC",
      "price": 49118.59,
      "timestamp": 1771078231,
      "round_id": 1024,
      "time_info": {
        "utc": "2026-02-14 14:10:31 UTC",
        "et": "2026-02-14 09:10:31 EST"
      }
    }
  ],
  "source": "enhanced"
}
```

### 4. Информация о времени
```bash
GET /timezones
```
**Ответ:**
```json
{
  "current_timestamp": 1771078413,
  "time_info": {
    "utc": "2026-02-14 14:13:33 UTC",
    "et": "2026-02-14 09:13:33 EST",
    "buffer_max_age_seconds": 60,
    "data_retention_hours": 12
  }
}
```

## 🎯 Ключевые улучшения

### 1. Буферизация (Решает вашу проблему!)
**Проблема:** Цена сгенерирована секунду назад, но еще не записана в файл → API не находит

**Решение:** 
- Цены сначала сохраняются в **in-memory buffer** (60 секунд)
- При запросе **сначала ищет в буфере**
- Если не найдено → ищет в файле
- **Всегда возвращает самую актуальную цену!**

**Пример:**
```
14:10:30 - Генерируем цену BTC: $50,000
14:10:31 - Сохраняем в буфер
14:10:32 - Сохраняем в файл
14:10:33 - Запрос: "Цена BTC на 14:10:30"
→ Сначала ищем в буфере → Найдено! → Возвращаем $50,000 ✅
```

### 2. ET Time Support
- Все временные метки конвертируются в ET
- API показывает время в обоих форматах
- Удобно для торговли на американских рынках

### 3. Гибкая очистка
- Можно настроить retention от 6 до 12 часов
- Очистка учитывает ET время
- Автоматическая очистка каждые 10 минут

## 🧪 Тестирование

### Запуск тестов
```bash
python test_enhanced_collector.py
```

### Тесты проверяют:
- ✅ Конвертацию времени UTC ↔ ET
- ✅ Работу in-memory buffer
- ✅ Приоритет буфера над файлом
- ✅ Enhanced file storage с ET временем
- ✅ API с новыми функциями

## 🔧 Управление сервисом

### Остановка
```bash
ssh root@danunahbot.ru "docker stop pricecollector-service"
```

### Запуск
```bash
ssh root@danunahbot.ru "docker run -d --name pricecollector-service -p 8080:8080 -v /app/data:/app/data -v /app/logs:/app/logs pricecollector-service-eqkne3:latest"
```

### Перезапуск
```bash
ssh root@danunahbot.ru "docker restart pricecollector-service"
```

### Проверка статуса
```bash
ssh root@danunahbot.ru "docker ps"
```

### Просмотр логов
```bash
ssh root@danunahbot.ru "docker logs pricecollector-service"
```

## 📈 Примеры использования

### Получение цены 1 минуту назад
```bash
curl -X GET "http://danunahbot.ru:8080/price/BTC?timestamp=$(($(date +%s) - 60))&tolerance=60"
```

### Получение цены 5 минут назад
```bash
curl -X GET "http://danunahbot.ru:8080/price/BTC?timestamp=$(($(date +%s) - 300))&tolerance=60"
```

### Получение цены в конкретное ET время
```bash
# Узнать timestamp для 09:00:00 ET
date -d "today 09:00:00 EST" +%s
# Использовать в запросе
curl -X GET "http://danunahbot.ru:8080/price/BTC?timestamp=1771060800&tolerance=60"
```

## 🎉 Преимущества

1. **Актуальные данные**: Буферизация гарантирует, что вы получите самую свежую цену
2. **ET время**: Удобно для торговли на американских рынках
3. **Гибкая очистка**: Настройте retention под свои нужды
4. **Производительность**: Буфер ускоряет запросы к свежим данным
5. **Надежность**: Резервирование в файле + оперативность буфера

## 🚀 Готово к использованию!

Теперь ваш коллектор:
- ✅ **Поддерживает ET время**
- ✅ **Имеет буферизацию для актуальных данных**
- ✅ **Решает проблему с ценами, которые еще не записаны в файл**
- ✅ **Имеет улучшенную систему очистки**
- ✅ **Полностью функционирует на VPS**

**Коллектор полностью готов к использованию!** 🎯